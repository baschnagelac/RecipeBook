@model RecipeBook.Models.RecipeBookM

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>RecipeBookM</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <input type="hidden" asp-for="RBUserId" />
            <div class="form-group">
                <label asp-for="Title" class="control-label"></label>
                <input asp-for="Title" class="form-control" />
                <span asp-validation-for="Title" class="text-danger"></span>
            </div>
@*            <div class="form-group">
                <label asp-for="RBUserId" class="control-label"></label>
                <select asp-for="RBUserId" class="form-control" asp-items="ViewBag.RBUserId"></select>
                <span asp-validation-for="RBUserId" class="text-danger"></span>
            </div>*@
            <div class="form-group">
                <label asp-for="RecipeId" class="control-label">Recipes</label>
                <select asp-for="RecipeId" class="form-control" asp-items="ViewBag.RecipeList"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
